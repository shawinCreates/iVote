[build]
  base    = "frontend"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Netlify's Next.js runtime handles SSR, image optimisation, and routing.
[[plugins]]
  package = "@netlify/plugin-nextjs"
